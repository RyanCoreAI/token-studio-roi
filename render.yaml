services:
  - type: web
    name: token-studio-roi
    env: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: PORT
        value: 4173
      - key: HOST
        value: 0.0.0.0
      - key: INGEST_TOKEN
        generateValue: true
    disk:
      name: token-studio-roi-data
      mountPath: /app/data
      sizeGB: 1
